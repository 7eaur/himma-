# تحديث تنفيذ المحتوى القانوني والصوت — 2026-09-10

هذا سجل متابعة تنفيذي مكمل لـ `CANONICAL_CONTENT_EXECUTION_CHECKPOINT_2026-09-09_AR.md` وملف التسليم الرئيسي. لا يعيد التحليل من الصفر، وGit الحالي هو الحقيقة التنفيذية.

## الحالة التنفيذية عند إنشاء هذا السجل

- الفرع: `integration/canonical-content-2026-09-08`.
- آخر HEAD تنفيذي قبل هذا السجل: `05fec87fa57aaa855615d45e94564e60fd30ae3a`.
- لا Merge ولا Deploy.
- لا Docker؛ بوابات CI/M09 تستخدم خدمات أصلية على runner.

## ما تم إصلاحه في الشريحة الحالية

1. فصل الصوت المعلق/غير المحسوم عن التنقل العادي والتكيف داخل المستوى، مع حجز التعليق فقط عند حدود الترقية أو إكمال المستوى الثالث.
2. جعل `rerecord_required` مهمة صريحة مؤجلة للطالب، ولا تعود الجولة الأصلية للتنفيذ إلا بعد فتح الطالب المهمة صراحة.
3. حفظ تاريخ التسجيلات غير الصالحة وعدم استبدال السجل القديم؛ إعادة التسجيل تنشئ `AudioSubmission` جديدًا.
4. تثبيت أن `graded` فقط يدخل كدليل أكاديمي للتكيف.
5. إصلاح مطابقة الصوت دلاليًا: عزل قصص `INS-*` عن مطابقة الحروف، وتفضيل التطابق الدقيق قبل المطابقة المرنة للكلمات.
6. تثبيت صوت القصة كوسيط على مستوى سياق النشاط، وليس إعادة تشغيله في كل سؤال من أسئلة الفهم.
7. إعادة دمج المحفزات المرئية المعتمدة سابقًا التي لم يغيّرها اعتماد 08 سبتمبر داخل المصدر القانوني نفسه بدل الاعتماد على Projection/Overlay قديم.
8. دمج تصحيح `POST-Q14` المعتمد في Student Experience v2 إلى كلمة «نخلة» وتسلسل `[ن، خ، ل، ة]` داخل الـCompiler القانوني.
9. دمج وسائط `L3-REIN-10` المولدة والمعتمدة (`HIMMA-GEN-SEQ-007..010`) داخل `STEP_MEDIA` القانوني، مع aliases دلالية صريحة فقط للألفاظ المختصرة المعتمدة.
10. تحديث اختبارات الانحدار القديمة لتتحقق من العقد القانوني الحالي بدل مفاتيح Overlay المتقاعدة.

## أدلة الاختبار المتحققة قبل آخر مصالحة

على تشغيل Backend سابق فعلي: جُمعت 825 حالة؛ نجحت 812 وفشلت 13. اختبارات حدود الصوت الثلاثة الجديدة نجحت، وكانت الإخفاقات المتبقية محصورة في عقود محتوى/وسائط واختبارات قديمة جرى تحليلها وإصلاح مصدرها في الشريحة الحالية.

كما ثبت سابقًا على SHA تنفيذي سابق من نفس السلسلة:

- Security: ناجح.
- Frontend: TypeScript + ESLint + Unit Tests + Production Build ناجحة.
- M04 Responsive: ناجح.
- M09: ناجح بخدمات PostgreSQL/Redis/MinIO أصلية ودون Docker، وشمل seed-twice وreadiness والنسخ الاحتياطي/الاستعادة.

هذه النتائج لا تُستخدم لإعلان HEAD الحالي Green؛ يجب إعادة تشغيل البوابات على HEAD الحالي بعد المصالحة الأخيرة.

## البند المفتوح الآن

إعادة تشغيل جميع البوابات الفعلية على HEAD الحالي وإصلاح أي إخفاق حتى تمر:

- Backend + migrations + model drift.
- Seed twice على PostgreSQL.
- Media + audio contracts.
- Security.
- Frontend type/lint/unit/build.
- Integration.
- E2E الفعلي لمسار الطالب والصوت والمراجعة وإعادة التسجيل.
- M04 responsive.
- M09 release readiness.

لا يتم الدمج أو النشر قبل اكتمال هذه الأدلة على نفس HEAD أو HEAD أحدث موثق ناتج فقط عن إصلاحات هذه البوابات.
